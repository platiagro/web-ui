export const getTotalMarketplaceTasks = ({ marketplaceReducer }) => {
  return marketplaceReducer.totalMarketplaceTasks;
};

export const getMarketplaceTasks = ({ marketplaceReducer }) => {
  return marketplaceReducer.marketplaceTasks;
};
